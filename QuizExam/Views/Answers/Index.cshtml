@model IEnumerable<QuizExam.Data.Answer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ItemOption.text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quiz.userName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ItemOption.text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quiz.userName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.answerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.answerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.answerID })
        </td>
    </tr>
}

</table>
